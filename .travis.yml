language: c
compiler:
        - clang
        - gcc
os:
        - linux
        - osx
env:
        - ARCH=native
        - ARCH=mingw32
        - ARCH=mingw64
matrix:
        include:
                - compiler: clang
                        os: osx
                       env: ARCH=native
                - compiler: gcc
                        os: osx
                       env: ARCH=native
                - compiler: clang
                        os: linux
                       env: ARCH=native
                - compiler: gcc
                        os: linux
                       env: ARCH=native
                - compiler: gcc
                        os: linux
                       env: ARCH=mingw32
                - compiler: gcc
                        os: linux
                       env: ARCH=mingw64

script:
        "./scripts/travis"
